class Contour:
    ''' Tools contour required for all flows to work

        Responsibilities:
            - parse config files (global and project if exists) and collect all tools required for flows
            - load required tools
            - check that all tools required for the flow are available

        Test Strategy:
            - TBD
    '''
    pass
